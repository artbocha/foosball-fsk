export type Player = {
  city: string;
  id: number;
  first_name: string;
  last_name: string;
};

export type PlayerState = {
  evks_rank: string;
  is_evks_player_active: boolean;
  player_name: string;
  rating: number;
};
